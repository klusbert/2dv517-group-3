#AUTO_GENERATED BY TERRAFORM DO NOT MODIFY THE OUTPUT


127.0.0.1 localhost

${db_master.access_ip_v4} ${db_master.name}
${db_slave.access_ip_v4} ${db_slave.name}
${db_proxy.access_ip_v4} ${db_proxy.name}
${monitoring.access_ip_v4} ${monitoring.name}
${fileserver.access_ip_v4} ${fileserver.name}
${load_balancer.access_ip_v4} ${load_balancer.name}

%{ for index, server in word_press ~}
${server.access_ip_v4} ${server.name} 
%{ endfor ~}

# The following lines are desirable for IPv6 capable hosts
::1 ip6-localhost ip6-loopback
fe00::0 ip6-localnet
ff00::0 ip6-mcastprefix
ff02::1 ip6-allnodes
ff02::2 ip6-allrouters
ff02::3 ip6-allhosts
